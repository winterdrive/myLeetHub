# Write your MySQL query statement below
select a.customer_id as 'customer_id' , count(a.visit_id) as 'count_no_trans' from visits as a left join transactions as b on a.visit_id = b.visit_id where amount is null group by a.customer_id 